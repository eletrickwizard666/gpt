# Visual Regression Placeholders

Playwright visual specs will live here once the workspace is wired to a running dev server. Use `npx playwright test` after starting `npm run dev` to capture golden snapshots.
